redis-lru is an implementation of a Least Recently Used cache using the sorted set data structure in redis. 

O(log n) insert and O(1) retrieval times.

License: BSD
Author: Vivek Narayanan <vivek_n@me.com>
